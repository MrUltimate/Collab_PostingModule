// Wait for document to be ready before running code
$(document).ready(function() {
  //Activate form from Submit Button Click
  $(".more").click(function() {
    // This dictates the Submit Button State (Active or Non-Active)
    // Replace "toggle" with the class the changes the button state in your CSS
    //$(this).toggleClass("toggle");
    // This dictates whether the form is in view or not (Active or Non-Active)
    // Change "active" to the class that activates the form view in your CSS
    //$('.submit').toggleClass("active");
  });
});
